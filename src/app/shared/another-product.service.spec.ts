import { AnotherProductService } from './another-product.service';

describe('AnotherProductService', () => {
  it('should create an instance', () => {
    const service = new AnotherProductService();
    expect(service).toBeTruthy();
  });
});
