import { LoggerService } from './logger.service.spec';

describe('LoggerService', () => {
  it('should create an instance', () => {
    const service = new LoggerService();
    expect(service).toBeTruthy();
  });
});
